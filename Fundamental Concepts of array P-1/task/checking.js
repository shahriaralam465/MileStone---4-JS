/**
 * 4. Checking if it's an Array
 * 
 * Create different variables, each containing either an array or a non-array value.
 * 
 * Now use isArray to check if each variable is an array.
 * 
 * Print a message to the console indicating whether each variable is an array or not.
 */


let books = ['Industrial Management', 'Principal of marketing', 'Database Management system', 'JavaScript', 'surveillance and security system'];

const fruit = ['Banana', 'Jackfruit', 'Berry', 'Mango', 'Lichi']

let place = ['bandorban', 'sajek', 'lauwachora'];

let AreGood = true;

console.log(Array.isArray(books));
console.log(Array.isArray(fruit));
console.log(Array.isArray(place));
console.log(Array.isArray(AreGood));
