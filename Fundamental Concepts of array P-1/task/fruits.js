/**
 * Declare an array
 * 
 * Declare an array with 5 elements containing fruits
 * console log the 3rd index element
 * change the value of the 2nd index element to jambura
 * console log the final array
 */


// Declare the array with 5 different fruits
const fruit = ['Banana', 'Jackfruit', 'Berry', 'Mango', 'Lichi']

// job done by console 3rd index element
console.log(fruit[3]);

// Changed The value of the 2nd index element to jambura
fruit[2]='jambura';

// Console log the final array
console.log(fruit);