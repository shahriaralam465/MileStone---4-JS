/**
 * 3. Checking Array Membership with ‘includes’
 * Create an array of books containing different book.
 * Use the includes method to check if the array contains a javascript book.
 * Print a message to the console indicating whether the element is present in the array or not.
 */

let books = ['Industrial Management', 'Principal of marketing', 'Database Management system', 'JavaScript', 'surveillance and security sysmtem'];

console.log(books.includes('JavaScript'));


if (books.includes('JavaScript')){
    console.log('Yes, The JavaScript Books are Available in the set of books array')
}