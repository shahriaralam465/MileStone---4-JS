/**
 * 5. Combining Arrays.
 * Create two arrays of your choice.
 * Use the concat method to combine the two arrays into a new array.
 * Print both the original arrays and the combined array using console.log().
 */

// created 2 array
let arr1 = ['bandorban', 'sajek', 'lauwachora'];
let arr2 = ['Banana', 'Jackfruit', 'Berry', 'Mango', 'Lichi']

// combine both of it by concat
const add = arr1.concat(arr2);

// print both the original and combined array by console log
console.log(arr1, arr2, add);